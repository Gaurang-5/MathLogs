import type { RevenueOverviewData } from './types';
export function RevenueOverview({ data }: { data: RevenueOverviewData }) { const metrics = [{ label: 'Active subscriptions', value: data.metrics.activeSubscriptions }, { label: 'Expiring in 30 days', value: data.metrics.expiringSoon }, { label: 'Scheduled operations', value: data.metrics.pendingOperations }, { label: 'Failed operations', value: data.metrics.failedOperations }]; return <><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(metric => <div key={metric.label} className="rounded-[24px] border border-stone-200 bg-[#fffdf9] p-5 shadow-sm"><p className="text-3xl font-black">{metric.value}</p><p className="mt-1 text-sm font-semibold text-stone-500">{metric.label}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{data.byPlan.map(item => <div key={item.plan} className="rounded-2xl border border-stone-200 bg-white p-4"><p className="text-xs font-black text-stone-400">{item.plan}</p><p className="mt-2 text-2xl font-black">{item.institutes}</p></div>)}</div><p className="mt-4 text-xs font-semibold text-stone-500">{data.revenueDefinition}</p></>; }
