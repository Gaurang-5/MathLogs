import type { SubscriptionItem } from './types';
export function SubscriptionTable({ items, onOperate }: { items: SubscriptionItem[]; onOperate: (item: SubscriptionItem) => void }) { return <div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead><tr className="border-b border-stone-200 text-[10px] font-black uppercase text-stone-400"><th className="px-5 py-3">Institute</th><th className="px-4 py-3">Plan</th><th className="px-4 py-3">Started</th><th className="px-4 py-3">Expires</th><th className="px-5 py-3" /></tr></thead><tbody>{items.map(item => <tr key={item.instituteId} className="border-b border-stone-100 last:border-0"><td className="px-5 py-4"><p className="font-black">{item.name}</p><p className="text-xs text-stone-500">{item.teacherName || item.status}</p></td><td className="px-4 py-4"><span className="rounded-full bg-stone-100 px-2.5 py-1 text-xs font-black">{item.plan}</span></td><td className="px-4 py-4 text-sm">{item.planStartDate ? new Date(item.planStartDate).toLocaleDateString() : '—'}</td><td className="px-4 py-4 text-sm">{item.planExpiryDate ? new Date(item.planExpiryDate).toLocaleDateString() : '—'}</td><td className="px-5 py-4 text-right"><button onClick={() => onOperate(item)} className="rounded-xl bg-stone-950 px-3 py-2 text-xs font-black text-white">Manage billing</button></td></tr>)}</tbody></table></div>; }
