{
    "name": "mathlogs-app",
    "version": "1.0.0",
    "keywords": [],
    "author": "",
    "license": "ISC",
    "scripts": {
        "setup": "npm install && cd server && npm install && cd ../client && npm install && cd ../mobile && npm install",
        "dev": "./dev.sh",
        "build": "cd client && npm run build && cd ../server && npm run build",
        "start": "cd server && npm start",
        "heroku-postbuild": "echo 'Installing Client...' && cd client && npm install --production=false && npm run build && echo 'Installing Server...' && cd ../server && npm install --production=false && npm run build"
    },
    "engines": {
        "node": "20.x"
    },
    "dependencies": {
        "@sentry/node": "^10.38.0",
        "lucide-react-native": "^0.577.0"
    }
}
